conda create --name BERT11.8 python=3.10   #自己設定要python幾
conda env remove --name BERT11.8

conda install pytorch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 pytorch-cuda=11.8 -c pytorch -c nvidia
conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch-nightly -c nvidia  

conda env export -n envA > environment.yaml
conda env create -n envB -f environment.yaml

conda activate base
conda install -c conda-forge jupyterlab ipywidgets nodejs

pip install jupyter
pip install ipython
pip install ipykernel
ipython kernel install --user --name=BERT11.8
python3 -m ipykernel install --user --name BERT11.8 --display-name "BERT"

jupyter kernelspec list
jupyter kernelspec uninstall bert11.8

pip install scikit-learn
pip install matplotlib
pip install transformers
pip install sentencepiece
pip install protobuf
pip install nltk
pip install seaborn
pip install umap
pip install tqdm
pip install --upgrade jupyter ipywidgets


conda clean --packages

jupyter nbconvert --to python ---.ipynb